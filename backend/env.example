# Backend Environment Variables
PORT=5001
NODE_ENV=development

# Email Configuration (for nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Production URLs (update these for production)
PRODUCTION_FRONTEND_URL=https://yourdomain.com
PRODUCTION_BACKEND_URL=https://yourdomain.com/api

# MongoDB Configuration
# For local development:
MONGODB_URI=mongodb://localhost:27017/vip-living-centers
# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vip-living-centers 